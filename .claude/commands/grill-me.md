Interview me relentlessly about every aspect of this plan until we reach a shared understanding. Walk down each branch of the design tree, resolving dependencies between decisions one-by-one. For each question, provide your recommended answer.

Ask the questions one at a time.

If a question can be answered by exploring the codebase, explore the codebase instead.

Once we have reached a shared understanding and all branches are resolved, synthesize the entire session into a task document and save it to `.claude/tasks/` as a new Markdown file. Name the file using the format `YYYY-MM-DD-<short-slug>.md` where the slug is a kebab-case summary of the topic (e.g. `2026-06-03-add-auth-flow.md`).

The task document should include:

- A short title and summary of the plan
- Key decisions made and the rationale behind each
- Any open questions or risks identified
- A checklist of concrete next steps
